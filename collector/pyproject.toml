[project]
name = "momentarily-collector"
version = "0.1.0"
description = "Local Docker container that captures MTA alert and E&E data for offline modeling"
requires-python = ">=3.13"

dependencies = [
    "httpx>=0.27",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
