.venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.ruff_cache/
.pyright/
.coverage
coverage.xml
htmlcov/
junit.xml
dist/
build/
.build-sha
.DS_Store

# Local secrets — never commit
.env
.env.local

# murk runtime lock (encrypted vault .murk IS committed; lockfile is local-only)
.murk.lock

# wrangler local cache
.wrangler/

# Shadow-review artifacts (generated locally by training.review; not for upstream)
docs/review/

# Local tooling (not for upstream)
AGENTS.md
CLAUDE.md
.claude/
.beads/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
